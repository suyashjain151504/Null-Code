# Null-Code
Null Code is a fully local, open-source AI coding agent. It can understand your entire codebase, plan and execute tasks, read and edit files, run terminal commands, perform web searches, maintain memory across sessions, and spawn sub-agents for complex work. The agent features compaction, and loop detection to handle long-running tasks efficiently.
